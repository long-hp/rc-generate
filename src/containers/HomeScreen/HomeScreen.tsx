
import React, { FC } from 'react';
import styles from './containers/HomeScreen.module.scss';

export interface containers/HomeScreenProps {}

const containers/HomeScreen: FC<containers/HomeScreenProps> = () => {
  return (
    <div>containers/HomeScreen</div>
  )
}

export default containers/HomeScreen;
